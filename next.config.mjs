/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lzwbsbnfbirgzayobuhh.supabase.co'], // Agrega el dominio aquí
    },
  };
  
  export default nextConfig;

