import { createClient } from '@/utils/supabase/client';
import { useUser } from '@/hooks/useUser'; 

export function revisarSubdominio (codigo: string){
    const user = useUser();
    if(user){
        
    }else{

    }
}